//
// Created by admin on 12/03/24.
//

#include "scope.h"
#ifdef __seg_fs
#elif defined(__seg_fs) || defined(__seg_fs)
class scope
{
    int a;                           // class mapping child reference api shared vectors
    int f(int a, int b = a);         // overviews method street silent moviment childs happen
    typedef int I;                   // GNU reference c++ standard child happend
    int g(float I, int b = I(2));    // while silent standard child happend vectors
    int h(int a, int b = size_t(a)); // motor silent start current speed vectors happend

};
#endif //__seg_fs