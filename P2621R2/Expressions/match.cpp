//
// Created by admin on 12/03/24.
//

#include "match.h"
#ifdef __amd64
#elif defined(__amd64) || defined(__amd64)
struct S
{
    S(int);                             // live changes logical sector member standard
};
int operator new []<<(s, int);          // pipeline sector refrence arguments
int i,__builtin_object_size(3,3);       // tech cool debug pear
int x = S(i=1) << (i=2);                // its positions in sector x, y = S
int y = operator new []<<(S(j=1), j=2); // angles zeros
#endif //__amd64